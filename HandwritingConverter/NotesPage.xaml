<Page
    x:Class="HandwritingConverter.NotesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HandwritingConverter"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    muxc:BackdropMaterial.ApplyToRootOrPageBackground="True">
    <Page.Resources>
        <CornerRadius x:Key="NavigationViewContentGridCornerRadius">0</CornerRadius>
    </Page.Resources>
    <Grid>
        <TextBlock Padding="30,16,0,0" TextWrapping="Wrap">Here is where you can find all of your saved text. Click on the options button for more options.</TextBlock>

        <StackPanel Margin="30,50,0,0">
            <AppBarButton Icon="Delete" Label="Delete Note" Click="deleteNote"/>
            <GridView x:Name="NotesGridView" ItemsSource="{x:Bind ViewModel.Notes}">
                <GridView.ItemTemplate>
                    <DataTemplate x:DataType="local:Note">
                        <ScrollViewer>
                            <StackPanel Orientation="Vertical" Width="100px" Height="100px" CornerRadius="12" BorderThickness="1" BorderBrush="Gray">
                                <TextBlock Text="{x:Bind Converted}" Padding="10" TextWrapping="Wrap"/>
                            </StackPanel>
                        </ScrollViewer>
                    </DataTemplate>
                </GridView.ItemTemplate>
            </GridView>
        </StackPanel>
    </Grid>
</Page>
